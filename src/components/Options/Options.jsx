import s from './Options.module.css'

export default function Options () {
    return (
        <div className={s.btnOptions}>
            <button>
                Good
            </button>
      </div> 
    )
}